# Field Task Location and Report Fix

Updated behavior:

- Excel `service type` uses the service type created by admin.
- Excel `Remarks` uses the first input field from the latest completed work update.
- Every work update now requires browser location permission before submit.
- Every update in the timeline shows captured location and live Google Maps link when saved.
- Workspace task cards and dashboard task rows show latest update location link when available.
