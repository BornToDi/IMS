{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm --workspace apps/api run prisma:generate && npm --workspace apps/web run build",
  "outputDirectory": "apps/web/.next"
}
